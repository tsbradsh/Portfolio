---
phase: 01-infrastructure-and-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _archive/index.html
  - _archive/scriptGL.js
  - _archive/styles.css
  - _archive/shader.glsl
  - next.config.ts
  - tsconfig.json
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - public/fonts/CascadiaCode.woff2
  - .prettierrc
  - .eslintrc.json
  - package.json
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
must_haves:
  truths:
    - "Running `next build` produces an `out/` directory with valid static HTML"
    - "CSS custom properties for color, spacing, typography, glow, and transitions are defined in globals.css and usable across all future components"
    - "Cascadia Code renders in the browser via `--font-mono` with no font flash or layout shift"
    - "TypeScript compiles without errors from a clean checkout"
  artifacts:
    - path: "next.config.ts"
      provides: "Static export configuration for Netlify"
      contains: "output: 'export'"
    - path: "app/globals.css"
      provides: "Complete design token system (22 tokens)"
      contains: "--color-teal: #00ffcc"
    - path: "app/layout.tsx"
      provides: "Root layout with Cascadia Code font variable on html element"
      contains: "cascadiaCode.variable"
    - path: "public/fonts/CascadiaCode.woff2"
      provides: "Self-hosted Cascadia Code font file"
    - path: "app/page.tsx"
      provides: "Placeholder home page for build verification"
  key_links:
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "import './globals.css'"
      pattern: "import.*globals\\.css"
    - from: "app/layout.tsx"
      to: "public/fonts/CascadiaCode.woff2"
      via: "localFont src path"
      pattern: "fonts/CascadiaCode"
    - from: "app/globals.css"
      to: ":root custom properties"
      via: "CSS custom properties on :root"
      pattern: "--color-bg.*#0a0a0a"
---

<objective>
Scaffold the complete Next.js 15 App Router project with static export, establish the full design token system, and load Cascadia Code font -- delivering the shared foundation that all subsequent phases inherit.

Purpose: Every later phase depends on this build pipeline, token system, and font. Getting it right once means all future work inherits correct defaults.
Output: A `next build` that produces `out/` with static HTML, `globals.css` with 22+ design tokens, Cascadia Code rendering via `--font-mono`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-infrastructure-and-foundation/01-CONTEXT.md
@.planning/phases/01-infrastructure-and-foundation/01-RESEARCH.md
@.planning/phases/01-infrastructure-and-foundation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 15 App Router with static export and tooling</name>
  <files>
    _archive/index.html, _archive/scriptGL.js, _archive/styles.css, _archive/shader.glsl,
    next.config.ts, tsconfig.json, package.json, .prettierrc, .eslintrc.json
  </files>
  <read_first>
    index.html, scriptGL.js, styles.css, shader.glsl,
    .planning/phases/01-infrastructure-and-foundation/01-RESEARCH.md,
    .planning/phases/01-infrastructure-and-foundation/01-CONTEXT.md
  </read_first>
  <action>
    **Step 1 — Archive existing vanilla files.**
    Create `_archive/` directory. Move these files into it (git mv to preserve history):
    - `index.html` -> `_archive/index.html`
    - `scriptGL.js` -> `_archive/scriptGL.js`
    - `styles.css` -> `_archive/styles.css`
    - `shader.glsl` -> `_archive/shader.glsl`

    Do NOT move `assets/`, `CLAUDE.md`, `README.md`, or `.planning/`. Only move the four vanilla source files.

    **Step 2 — Scaffold Next.js.**
    Run:
    ```bash
    npx create-next-app@latest . --typescript --eslint --app --no-tailwind --no-src-dir --import-alias "@/*"
    ```
    If prompted about overwriting files, allow it -- the vanilla files are already archived.

    **Step 3 — Configure next.config.ts for static export (per D-15, D-16).**
    Replace the generated `next.config.ts` with exactly:
    ```ts
    import type { NextConfig } from 'next'

    const nextConfig: NextConfig = {
      output: 'export',
      images: {
        unoptimized: true,
      },
    }

    export default nextConfig
    ```

    **Step 4 — Verify tsconfig.json has strict mode enabled.**
    Ensure `"strict": true` is present. Add `"_archive"` to the `"exclude"` array so archived JS files do not cause type errors. The generated tsconfig from create-next-app should already have most settings correct. Ensure paths alias exists: `"@/*": ["./*"]`.

    **Step 5 — Install Three.js (installed now, used in Phase 2).**
    ```bash
    npm install three
    npm install -D @types/three
    ```

    **Step 6 — Add Prettier (per RESEARCH.md).**
    Install:
    ```bash
    npm install -D prettier eslint-config-prettier
    ```

    Create `.prettierrc` with exactly:
    ```json
    {
      "semi": false,
      "singleQuote": true,
      "trailingComma": "es5",
      "tabWidth": 2,
      "printWidth": 100
    }
    ```

    Update `.eslintrc.json` (or ESLint config file created by create-next-app) to extend prettier. If the file is `.eslintrc.json`, set:
    ```json
    {
      "extends": ["next/core-web-vitals", "prettier"]
    }
    ```
    NOTE: Next.js 15 may generate `eslint.config.mjs` (flat config) instead of `.eslintrc.json`. If so, add the prettier config import to the flat config format. The key requirement is that eslint-config-prettier is active.

    **Step 7 — Verify build.**
    Run `npm run build`. Confirm it completes without errors and produces an `out/` directory. Run `npx tsc --noEmit` to confirm TypeScript compiles cleanly.
  </action>
  <verify>
    <automated>npm run build && test -d out && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `_archive/index.html` exists (vanilla files archived)
    - `_archive/scriptGL.js` exists
    - `_archive/styles.css` exists
    - `_archive/shader.glsl` exists
    - `next.config.ts` contains `output: 'export'`
    - `next.config.ts` contains `unoptimized: true`
    - `tsconfig.json` contains `"strict": true`
    - `tsconfig.json` exclude array contains `_archive`
    - `package.json` contains `"three"` in dependencies
    - `package.json` contains `"@types/three"` in devDependencies
    - `.prettierrc` contains `"singleQuote": true`
    - ESLint config extends `prettier`
    - `npm run build` exits 0
    - `out/` directory exists after build
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Next.js 15 App Router scaffolded with static export, Three.js installed, Prettier configured, build produces out/ directory, TypeScript compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Design token system in globals.css and Cascadia Code font pipeline</name>
  <files>
    app/globals.css, app/layout.tsx, app/page.tsx, public/fonts/CascadiaCode.woff2
  </files>
  <read_first>
    app/globals.css, app/layout.tsx, app/page.tsx,
    .planning/phases/01-infrastructure-and-foundation/01-UI-SPEC.md,
    .planning/phases/01-infrastructure-and-foundation/01-CONTEXT.md
  </read_first>
  <action>
    **Step 1 — Download Cascadia Code font (per D-12).**
    Download the Cascadia Code woff2 font file from Microsoft's GitHub releases:
    ```bash
    mkdir -p public/fonts
    ```
    Download from `https://github.com/microsoft/cascadia-code/releases` — look for the latest release. Download the zip, extract the `woff2` directory. Place files at:
    - `public/fonts/CascadiaCode.woff2` (regular weight 400)
    - `public/fonts/CascadiaCode-SemiBold.woff2` (weight 600) if available as separate file

    If a variable font woff2 file is available (covers all weights in one file), use that single file instead and name it `public/fonts/CascadiaCode.woff2`.

    Use curl or similar CLI tool to download. Check the releases page for exact URLs. Example pattern:
    ```bash
    curl -L -o /tmp/cascadia.zip "https://github.com/microsoft/cascadia-code/releases/latest/download/CascadiaCode-2404.23.zip"
    ```
    The exact filename changes per release -- check the releases page at execution time.

    **Step 2 — Replace globals.css with complete token system (per D-01 through D-11, UI-SPEC token reference).**
    Replace the create-next-app default `app/globals.css` with exactly this content:

    ```css
    /* Phase 1 Design Token System — source: 01-UI-SPEC.md */
    /* All tokens defined on :root. No token defined anywhere else. */

    :root {
      /* Spacing (8px grid — D-05) */
      --space-half: 4px;
      --space-1: 8px;
      --space-2: 16px;
      --space-3: 24px;
      --space-4: 32px;
      --space-6: 48px;
      --space-8: 64px;
      --space-10: 80px;
      --space-16: 128px;

      /* Typography (D-12, D-13, D-14) */
      --text-sm: 14px;
      --text-base: 16px;
      --text-lg: 20px;
      --text-xl: 32px;

      /* Color (D-01, D-02, D-03, D-04) */
      --color-bg: #0a0a0a;
      --color-surface: #111111;
      --color-surface-nav: rgba(10, 10, 10, 0.95);
      --color-teal: #00ffcc;
      --color-teal-subtle: rgba(0, 255, 204, 0.15);
      --color-teal-glow: rgba(0, 255, 204, 0.20);
      --color-blue: #7dd3fc;
      --color-error: #ff4d4d;

      /* Glow and border (D-07, D-08, D-09) */
      --glow-teal: 0 0 8px rgba(0, 255, 204, 0.20);
      --glow-teal-nav: 0 0 20px rgba(0, 255, 204, 0.15);
      --radius-card: 4px;

      /* Transitions (D-10, D-11) */
      --transition-fast: 150ms ease;
      --transition-base: 250ms ease;
      --transition-slow: 400ms ease;
    }

    /* Global resets */
    *,
    *::before,
    *::after {
      box-sizing: border-box;
    }

    html,
    body {
      margin: 0;
      padding: 0;
      background-color: var(--color-bg);
      color: var(--color-teal);
      font-family: var(--font-mono), Consolas, 'Courier New', monospace;
      font-size: var(--text-base);
      line-height: 1.5;
    }
    ```

    This is the COMPLETE globals.css. Remove ALL create-next-app default styles (Geist font references, default page styles, etc.). The file must contain only the token system and global resets above.

    **Step 3 — Configure layout.tsx with Cascadia Code font variable (per D-12, D-13, D-14).**
    Replace `app/layout.tsx` with:

    ```tsx
    import type { Metadata } from 'next'
    import localFont from 'next/font/local'
    import './globals.css'

    const cascadiaCode = localFont({
      src: [
        {
          path: '../public/fonts/CascadiaCode.woff2',
          weight: '400',
          style: 'normal',
        },
        {
          path: '../public/fonts/CascadiaCode-SemiBold.woff2',
          weight: '600',
          style: 'normal',
        },
      ],
      variable: '--font-mono',
      display: 'swap',
    })

    export const metadata: Metadata = {
      title: 'Tyler Bradshaw — Developer Portfolio',
      description: 'Developer portfolio for Tyler Bradshaw',
    }

    export default function RootLayout({
      children,
    }: {
      children: React.ReactNode
    }) {
      return (
        <html lang="en" className={cascadiaCode.variable}>
          <body>{children}</body>
        </html>
      )
    }
    ```

    IMPORTANT: If only a single variable font file was available (Step 1), use the simpler single-src form instead:
    ```tsx
    const cascadiaCode = localFont({
      src: '../public/fonts/CascadiaCode.woff2',
      variable: '--font-mono',
      display: 'swap',
    })
    ```
    Adapt the src configuration to match whichever font files were actually downloaded. The critical invariants are: `variable: '--font-mono'`, `display: 'swap'`, and `className={cascadiaCode.variable}` on the `<html>` element.

    **Step 4 — Create placeholder page.tsx.**
    Replace `app/page.tsx` with:

    ```tsx
    export default function Home() {
      return (
        <main style={{ padding: 'var(--space-4)', color: 'var(--color-teal)' }}>
          <h1 style={{ fontSize: 'var(--text-xl)', fontWeight: 600 }}>
            Tyler Bradshaw
          </h1>
          <p>Portfolio under construction.</p>
        </main>
      )
    }
    ```

    **Step 5 — Delete any leftover create-next-app boilerplate.**
    Remove these files if they exist (created by create-next-app but not needed):
    - `app/page.module.css` (default page styles)
    - `public/next.svg` (default Next.js logo)
    - `public/vercel.svg` (default Vercel logo)
    - Any `public/*.svg` files that came from create-next-app
    - `app/favicon.ico` — keep this one (or replace later), but do not delete it

    **Step 6 — Final build verification.**
    Run:
    ```bash
    npm run build
    npx tsc --noEmit
    ```
    Both must exit 0. The `out/` directory must contain an `index.html` file. Check that the generated HTML includes a font preload hint for Cascadia Code.
  </action>
  <verify>
    <automated>npm run build && test -f out/index.html && npx tsc --noEmit && grep -c "\\-\\-color-teal" app/globals.css && grep -c "\\-\\-space-1" app/globals.css && grep -c "\\-\\-transition-fast" app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `public/fonts/` directory contains at least one `.woff2` file
    - `app/globals.css` contains `--space-half: 4px`
    - `app/globals.css` contains `--space-1: 8px`
    - `app/globals.css` contains `--space-16: 128px`
    - `app/globals.css` contains `--text-sm: 14px`
    - `app/globals.css` contains `--text-base: 16px`
    - `app/globals.css` contains `--text-lg: 20px`
    - `app/globals.css` contains `--text-xl: 32px`
    - `app/globals.css` contains `--color-bg: #0a0a0a`
    - `app/globals.css` contains `--color-surface: #111111`
    - `app/globals.css` contains `--color-teal: #00ffcc`
    - `app/globals.css` contains `--color-teal-subtle: rgba(0, 255, 204, 0.15)`
    - `app/globals.css` contains `--color-blue: #7dd3fc`
    - `app/globals.css` contains `--color-error: #ff4d4d`
    - `app/globals.css` contains `--glow-teal: 0 0 8px rgba(0, 255, 204, 0.20)`
    - `app/globals.css` contains `--glow-teal-nav: 0 0 20px rgba(0, 255, 204, 0.15)`
    - `app/globals.css` contains `--radius-card: 4px`
    - `app/globals.css` contains `--transition-fast: 150ms ease`
    - `app/globals.css` contains `--transition-base: 250ms ease`
    - `app/globals.css` contains `--transition-slow: 400ms ease`
    - `app/globals.css` contains `--color-surface-nav: rgba(10, 10, 10, 0.95)`
    - `app/globals.css` contains `--color-teal-glow: rgba(0, 255, 204, 0.20)`
    - `app/globals.css` contains `font-family: var(--font-mono)`
    - `app/globals.css` contains `background-color: var(--color-bg)` (not hardcoded hex)
    - `app/layout.tsx` contains `variable: '--font-mono'`
    - `app/layout.tsx` contains `cascadiaCode.variable`
    - `app/layout.tsx` contains `<html lang="en"`
    - `app/layout.tsx` contains `import './globals.css'`
    - `app/layout.tsx` contains `display: 'swap'`
    - `app/page.tsx` contains `Tyler Bradshaw`
    - `app/page.module.css` does NOT exist (boilerplate removed)
    - `npm run build` exits 0
    - `out/index.html` exists
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 22 design tokens from UI-SPEC are defined in globals.css on :root, Cascadia Code loads via next/font/local with --font-mono CSS variable on html element, build produces out/ with valid HTML, no font flash or CLS</done>
</task>

</tasks>

<verification>
1. `npm run build` exits 0 and produces `out/` directory with `index.html`
2. `npx tsc --noEmit` exits 0 with zero type errors
3. `app/globals.css` contains all 22 tokens from UI-SPEC Phase 1 Token Reference
4. `app/layout.tsx` applies `cascadiaCode.variable` to `<html>` element (not `<body>`)
5. `public/fonts/` contains Cascadia Code woff2 file(s)
6. `_archive/` contains the four original vanilla files
7. `next.config.ts` has `output: 'export'` and `images: { unoptimized: true }`
8. Three.js and @types/three appear in package.json dependencies
</verification>

<success_criteria>
- `next build` produces a working static export in `out/`
- TypeScript compiles without errors
- All 22 design tokens are queryable via `var()` in CSS
- Cascadia Code font renders with no flash (--font-mono variable active on :root)
- Vanilla files preserved in `_archive/` for Phase 2 shader reference
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-and-foundation/01-01-SUMMARY.md`
</output>
